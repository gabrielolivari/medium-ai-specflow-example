# Acceptance Criteria

## AC-1 Required files exist

Given the sample story folder  
When a contributor opens it  
Then the folder contains `spec.md`, `acceptance-criteria.md`, `contracts/`, `test-plan.md`, `traceability.md`, and `changelog.md`.

## AC-2 Story is testable

Given the sample story  
When acceptance criteria are reviewed  
Then each criterion can be validated with explicit checks.
